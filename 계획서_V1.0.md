# 프로젝트 계획서

**프로젝트명:** 한국어 텍스트 격식 수준 및 톤 자동 분석 시스템  
**기간:** 8주 (2026.03 ~ 2026.06)  
**팀 구성:** 3명  

---

## 📋 목차
1. [팀 구성 및 역할 분담](#1-팀-구성-및-역할-분담)
2. [8주 마일스톤](#2-8주-마일스톤)
3. [형상관리 전략](#3-형상관리-전략)
4. [개발 환경 및 도구](#4-개발-환경-및-도구)
5. [의사소통 및 협업](#5-의사소통-및-협업)
6. [프로젝트 종료 후 인수인계](#6-프로젝트-종료-후-인수인계)

---

## 1. 팀 구성 및 역할 분담

### 👤 역할 1: 팀장 / Backend Lead

**주요 책임:** 전체 아키텍처 설계, 백엔드 개발, 프로젝트 관리

#### 핵심 업무

**1. 프로젝트 관리 (10%)**
- 주간 스프린트 계획 수립
- 일일 스탠드업 미팅 진행 (15분)
- 이슈 트래킹 및 우선순위 관리
- 멘토/기업과의 커뮤니케이션 창구

**2. 시스템 아키텍처 설계 (15%)**
- 전체 시스템 구조 설계
- API 엔드포인트 설계 (OpenAPI 스펙)
- 데이터베이스 스키마 설계
- 배포 아키텍처 (Docker Compose)

**3. Backend API 개발 (50%)**
```
담당 API:
- POST /api/analyze/formality (격식 분석)
- POST /api/analyze/tone (톤 분석)
- POST /api/analyze/consistency (일관성 체크)
- POST /api/analyze/full (종합 분석)
- GET /api/history (분석 이력)
- GET /api/stats (통계)

담당 모듈 (예시 구조):
- main.py (앱 진입점)
- routers/ (라우터)
- models/ (데이터 모델)
- database.py (DB 연결)
- config.py (설정)
```

**4. LLM API 연동 (15%)**
```
services/llm_service.py (예시)
- LLM 프롬프트 호출
- 응답 파싱
- 에러 핸들링
- 비용 최적화 (캐싱)
```

**5. 데이터베이스 관리 (10%)**
- DB 스키마 설계
- 마이그레이션 관리 (필요 시)
- 인덱싱 최적화
- 백업 스크립트

#### 주차별 작업

| 주차 | 작업 내용 |
|------|----------|
| Week 1 | 프로젝트 셋업, API 설계, DB 스키마 설계 |
| Week 2 | 기본 API 스켈레톤, DB 연결, 형태소 분석 통합 (AI Engineer와 협업) |
| Week 3 | LLM API 연동, 톤 분석 API 구현 |
| Week 4 | 일관성 체크 API, 종합 분석 API, 중간 발표 준비 |
| Week 5 | 분석 이력 API, 통계 API |
| Week 6 | 관리자 API (규칙 사전 CRUD), 성능 최적화 |
| Week 7 | API 테스트, 문서화, 버그 수정 |
| Week 8 | Docker 배포, 최종 통합 테스트, 발표 준비 |

#### 산출물
- [ ] API 스펙 문서 (OpenAPI/Swagger 형식 권장)
- [ ] API 테스트 코드 (단위 테스트, 커버리지 80%+)
- [ ] 데이터베이스 마이그레이션 스크립트 (필요 시)
- [ ] Docker Compose 설정
- [ ] 배포 가이드 (DEPLOYMENT.md)

---

### 👤 역할 2: AI/NLP Engineer

**주요 책임:** 형태소 분석, 격식 분석 로직, 일관성 체크 알고리즘

#### 핵심 업무

**1. 형태소 분석 엔진 구축 (30%)**
```python
# services/morphology_service.py

from kiwipiepy import Kiwi

class MorphologyAnalyzer:
    def __init__(self):
        self.kiwi = Kiwi()
    
    def analyze(self, text: str):
        """
        텍스트를 형태소 분석
        - 문장 분할
        - 형태소 태깅
        - 종결어미 추출
        """
        pass
    
    def extract_sentence_endings(self, text: str):
        """종결어미 추출 (~습니다, ~해요, ~해 등)"""
        pass
    
    def extract_honorific_markers(self, text: str):
        """높임 표현 추출 (~시, 께서, 드리다 등)"""
        pass
```

**담당:**
- Kiwi 형태소 분석기 통합
- 문장 분할 로직
- 종결어미 추출
- 높임 표현 마커 추출

**2. 규칙 기반 격식 분석 (25%)**
```python
# services/formality_analyzer.py

class FormalityAnalyzer:
    def __init__(self):
        self.ending_dict = self.load_ending_dictionary()
        # ~습니다: very-formal
        # ~해요: semi-formal
        # ~해: casual
    
    def analyze_formality(self, text: str):
        """
        격식 수준 분석
        1. 종결어미 카운팅
        2. 최다 빈도 격식 수준 결정
        3. 일관성 점수 계산
        """
        pass
    
    def calculate_consistency_score(self, distribution):
        """
        격식 분포 기반 일관성 점수
        90% 이상 한 종류 → 100점
        혼용 비율에 따라 점수 차감
        """
        pass
```

**담당:**
- 종결어미 사전 구축
- 격식 수준 판단 알고리즘
- 일관성 점수 계산 로직

**3. 일관성 체크 알고리즘 (20%)**
```python
# services/consistency_checker.py

class ConsistencyChecker:
    def check_formality_mixing(self, sentences):
        """격식 혼용 감지"""
        issues = []
        
        for i, sent in enumerate(sentences):
            formality = self.detect_formality(sent)
            # 이전/이후 문장과 비교
            if i > 0 and formality != prev_formality:
                issues.append({
                    'type': 'formality_mixing',
                    'severity': 'high',
                    'sentences': [prev_sent, sent]
                })
        
        return issues
    
    def check_honorific_target_consistency(self, text):
        """높임 대상 일관성 체크"""
        # 예: "김 부장님"은 높임, "이 부장님"은 비높임
        # → 불일치 감지
        pass
```

**담당:**
- 격식 혼용 감지 알고리즘
- 높임 대상 불일치 감지
- 문제 심각도 판단 (high/medium/low)

**4. 높임법 분석 (15%)**
```python
# services/honorific_analyzer.py

class HonorificAnalyzer:
    def analyze(self, text: str):
        """
        높임법 분석
        - 주체 높임 (~시, 께서)
        - 객체 높임 (드리다, 여쭙다)
        - 상대 높임 (종결어미)
        """
        pass
    
    def detect_honorific_errors(self, text: str):
        """
        높임법 오류 감지
        예: "사장님이 말했습니다" (주체 높임 누락)
        """
        pass
```

**5. LLM 프롬프트 설계 (10%)**
```
Backend Lead와 협업:
- 톤 분석 프롬프트 작성
- 높임법 오류 감지 프롬프트 작성
- 개선 제안 생성 프롬프트 작성
```

#### 주차별 작업

| 주차 | 작업 내용 |
|------|----------|
| Week 1 | Kiwi 설치 및 테스트, 형태소 분석 기본 로직 |
| Week 2 | 종결어미 사전 구축, 격식 분석 프로토타입 |
| Week 3 | 일관성 체크 알고리즘 v1, 높임법 분석 시작 |
| Week 4 | 일관성 체크 고도화, 중간 발표 준비 |
| Week 5 | 높임법 오류 감지 로직, LLM 프롬프트 튜닝 |
| Week 6 | 규칙 사전 확장, 엣지 케이스 처리 |
| Week 7 | 테스트 데이터 수집, 정확도 평가 (85%+ 목표) |
| Week 8 | 최종 튜닝, 문서화 |

#### 산출물
- [ ] 형태소 분석 모듈 (테스트 포함)
- [ ] 격식 분석 모듈 (테스트 포함)
- [ ] 일관성 체크 모듈 (F1 Score 0.8+)
- [ ] 종결어미 사전 (JSON/CSV)
- [ ] 높임 표현 사전 (JSON/CSV)
- [ ] 알고리즘 설명 문서 (ALGORITHM.md)

---

### 👤 역할 3: Frontend Developer

**주요 책임:** 웹 인터페이스, UI/UX 설계, 시각화

#### 핵심 업무

**1. UI/UX 설계 (15%)**
- 와이어프레임 작성 (Figma 등)
- 사용자 플로우 설계
- 컴포넌트 구조 설계
- 디자인 시스템 (UI 라이브러리 활용)

**2. 프로젝트 구조 (10%)**
```
frontend/ (예시 구조)
├── public/
├── src/
│   ├── components/
│   │   ├── common/          # 공통 컴포넌트
│   │   │   ├── Button
│   │   │   ├── Card
│   │   │   └── Badge
│   │   ├── analysis/        # 분석 관련
│   │   │   ├── TextInput
│   │   │   ├── ResultCard
│   │   │   ├── HighlightedText
│   │   │   └── IssueList
│   │   └── dashboard/       # 대시보드
│   │       ├── ScoreGauge
│   │       └── StatChart
│   ├── pages/
│   │   ├── HomePage
│   │   ├── AnalysisPage
│   │   └── AdminPage
│   ├── services/
│   │   └── api              # API 호출
│   ├── hooks/               # 커스텀 훅 (선택)
│   ├── types/               # 타입 정의 (TypeScript 사용 시)
│   └── App
└── package.json
```

**3. 핵심 페이지 개발 (50%)**

**3-1. 분석 페이지 (메인 페이지)**
```
화면 구성:
좌측: 텍스트 입력
- 직접 입력 영역 (TextArea)
- 파일 업로드 (드래그&드롭)
- 분석 버튼
- 로딩 인디케이터

우측: 결과 표시
- 종합 점수 카드
- 격식 수준 카드
- 톤 카드
- 일관성 문제 목록
```

**3-2. 결과 표시 컴포넌트**
```
필요한 컴포넌트:
- ScoreCard: 종합 품질 점수 (0~100)
- FormalityCard: 격식 수준 (very-formal ~ casual)
- ToneCard: 톤 (respectful, requesting 등)
- IssueList: 일관성 문제 목록
- ProgressBar: 점수 시각화
- Badge: 상태 표시
```

**3-3. 텍스트 하이라이트**
```
기능:
- 문제가 있는 문장/단어 컬러 마킹
- 빨강: 격식 혼용
- 주황: 높임법 오류
- 마우스 오버 시 툴팁 (문제 설명)
```

**4. 시각화 (15%)**
```
차트 컴포넌트:
- 격식 분포 (파이 차트)
- 점수 추이 (라인 차트, 선택)
- 문제 유형별 통계 (바 차트, 선택)

라이브러리 옵션:
- Recharts (권장)
- Chart.js
- D3.js (고급)
```

**5. API 연동 (10%)**
```javascript
// services/api (예시)
const API_BASE_URL = 'http://localhost:8000';

export const analyzeText = async (text) => {
  const response = await fetch(`${API_BASE_URL}/api/analyze/full`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ text })
  });
  return response.json();
};

export const getAnalysisHistory = async () => {
  const response = await fetch(`${API_BASE_URL}/api/history`);
  return response.json();
};
```

#### 주차별 작업

| 주차 | 작업 내용 |
|------|----------|
| Week 1 | React 프로젝트 셋업, UI 와이어프레임, 컴포넌트 구조 설계 |
| Week 2 | 공통 컴포넌트 개발 (Button, Card, Badge) |
| Week 3 | TextInput 컴포넌트, API 연동 로직 |
| Week 4 | 결과 표시 컴포넌트 (ScoreCard, FormalityCard), 중간 발표 준비 |
| Week 5 | 텍스트 하이라이트, IssueList 컴포넌트 |
| Week 6 | 시각화 (차트), 분석 이력 페이지 |
| Week 7 | 관리자 페이지 (규칙 사전 관리), UI 개선 |
| Week 8 | 반응형 디자인, 최종 UX 개선, 테스트 |

#### 산출물
- [ ] 웹 애플리케이션 (프론트엔드)
- [ ] UI/UX 와이어프레임 (Figma 등)
- [ ] 컴포넌트 라이브러리 (Storybook 등, 선택)
- [ ] 프론트엔드 테스트 (단위 테스트)
- [ ] 사용자 가이드 (USER_GUIDE.md)

---

## 2. 8주 마일스톤

### Week 1: 프로젝트 기획 및 환경 설정
**목표:** 개발 환경 완료, 프로젝트 구조 확립

#### 전체 팀
- [ ] 킥오프 미팅 (월요일)
  - 역할 및 책임 명확화
  - **기술 스택 최종 결정** (Backend 프레임워크, Frontend 프레임워크, DB 등)
  - 커뮤니케이션 채널 설정 (Slack, Discord 등)
  - 일일 스탠드업 시간 정하기 (매일 오전 10시, 15분)
- [ ] Git Repository 생성 및 초기 설정
- [ ] 개발 환경 통일 (Docker, IDE 설정)
- [ ] 프로젝트 관리 도구 설정 (GitHub Projects 또는 Notion)

#### Backend Lead
- [ ] 백엔드 프로젝트 구조 생성 (선택한 프레임워크 기준)
- [ ] 데이터베이스 Docker 컨테이너 설정 (선택한 DB 기준)
- [ ] API 엔드포인트 설계 (OpenAPI 스펙)
- [ ] 데이터베이스 스키마 설계
  ```sql
  -- 예시 스키마 (DB 선택에 따라 조정)
  -- analyses 테이블
  CREATE TABLE analyses (
    id SERIAL PRIMARY KEY,
    text TEXT NOT NULL,
    formality_level VARCHAR(20),
    formality_confidence FLOAT,
    tone VARCHAR(20),
    tone_confidence FLOAT,
    quality_score INT,
    created_at TIMESTAMP DEFAULT NOW()
  );
  
  -- formality_rules 테이블
  CREATE TABLE formality_rules (
    id SERIAL PRIMARY KEY,
    ending VARCHAR(50),
    formality_level VARCHAR(20),
    description TEXT
  );
  ```

#### AI/NLP Engineer
- [ ] Kiwi 설치 및 테스트
- [ ] 기본 형태소 분석 스크립트 작성
- [ ] 종결어미 사전 초안 작성 (30~50개)

#### Frontend Developer
- [ ] 프론트엔드 프로젝트 생성 (선택한 프레임워크 기준)
- [ ] UI 라이브러리 설치 및 테마 설정 (선택한 라이브러리 기준)
- [ ] 와이어프레임 작성 (Figma 등)
- [ ] 컴포넌트 구조 설계

**체크포인트 (금요일):**
- Git Repository 준비 완료
- 각자 개발 환경 작동 확인
- API 설계 문서 리뷰
- **기술 스택 문서화** (README에 기록)
- 다음 주 계획 확정

---

### Week 2: 기본 인프라 및 프로토타입
**목표:** 기본 API 작동, 형태소 분석 통합

#### Backend Lead
- [ ] 기본 라우터 구현
  ```
  예시 엔드포인트:
  POST /api/analyze/formality
    - 형태소 분석기 호출
    - 격식 분석 로직 호출
    - 결과 반환: {"formality_level": "formal", "confidence": 0.9}
  ```
- [ ] 데이터베이스 연결 및 ORM 설정 (선택한 DB/ORM 기준)
- [ ] 기본 CRUD API (분석 이력 저장/조회)

#### AI/NLP Engineer
- [ ] 형태소 분석 모듈 완성
  ```python
  # services/morphology_service.py (예시)
  from kiwipiepy import Kiwi
  
  def analyze(text: str):
      kiwi = Kiwi()
      tokens = kiwi.tokenize(text)
      return tokens
  ```
- [ ] 종결어미 추출 로직
- [ ] 격식 분석 프로토타입 v0.1
  - 종결어미 카운팅
  - 최다 빈도 격식 수준 결정

#### Frontend Developer
- [ ] 공통 컴포넌트 개발
  - Button, Card, Badge, TextField
- [ ] 기본 레이아웃 (Header, Container)
- [ ] TextInput 컴포넌트 (텍스트 입력 영역)

**체크포인트 (금요일):**
- 형태소 분석 작동 확인
- 간단한 텍스트 입력 → 격식 분석 → 결과 반환 (E2E 테스트)
- 팀 전체 코드 리뷰

---

### Week 3: 핵심 분석 로직 개발
**목표:** 격식 분석, 톤 분석 구현

#### Backend Lead
- [ ] LLM API 연동 (Anthropic Claude 또는 OpenAI)
  ```python
  # services/llm_service.py (예시)
  def analyze_tone(text: str) -> dict:
      prompt = f"다음 텍스트의 톤을 분석하시오: {text}"
      response = llm_client.create(...)  # API 호출
      return parse_response(response)
  ```
- [ ] 톤 분석 API 구현
- [ ] 에러 핸들링 및 로깅

#### AI/NLP Engineer
- [ ] 격식 분석 알고리즘 고도화
  - 일관성 점수 계산
  - 혼용 패턴 감지
- [ ] 높임법 분석 시작
  - 주체 높임 마커 추출 (~시, 께서)
  - 객체 높임 마커 추출 (드리다, 여쭙다)
- [ ] LLM 프롬프트 작성 (톤 분석용)

#### Frontend Developer
- [ ] 분석 실행 버튼 및 로딩 상태
- [ ] API 연동 (fetch 또는 axios)
- [ ] 결과 표시 컴포넌트 시작
  - ScoreCard (종합 점수)
  - FormalityCard (격식 수준)

**체크포인트 (금요일):**
- 격식 분석 정확도 테스트 (샘플 10개)
- 톤 분석 API 작동 확인
- 프론트엔드 결과 표시 확인

---

### Week 4: 일관성 체크 및 중간 발표 준비
**목표:** 일관성 감지, 종합 분석 API, 중간 발표

#### Backend Lead
- [ ] 일관성 체크 API 구현
  ```
  POST /api/analyze/consistency
    - 격식 혼용 감지
    - 문제 목록 반환
  ```
- [ ] 종합 분석 API (모든 분석 통합)
  ```
  POST /api/analyze/full
    - 격식 + 톤 + 일관성 분석
    - 종합 품질 점수 계산
    - 통합 결과 반환
  ```

#### AI/NLP Engineer
- [ ] 일관성 체크 알고리즘 v1 완성
  - 격식 혼용 감지
  - 문제 심각도 판단
- [ ] 테스트 케이스 작성 (20개)
- [ ] 정확도 평가 시작

#### Frontend Developer
- [ ] ToneCard 컴포넌트
- [ ] IssueList 컴포넌트 (일관성 문제 목록)
- [ ] 중간 발표 PPT 작성 (전체 팀 협업)

**체크포인트 (금요일) - 중간 발표:**
- 프로젝트 진행 상황 공유
- 라이브 데모 (텍스트 입력 → 분석 → 결과)
- 피드백 수집 및 반영 계획

---

### Week 5: 고급 기능 및 UI 개선
**목표:** 텍스트 하이라이트, 분석 이력, 시각화

#### Backend Lead
- [ ] 분석 이력 API
  ```
  GET /api/history?limit=10
    - 최근 분석 이력 조회
    - 시간순 정렬
  ```
- [ ] 통계 API
  ```
  GET /api/stats
    - 평균 품질 점수
    - 문서 유형별 통계 등
  ```

#### AI/NLP Engineer
- [ ] 높임법 오류 감지 고도화
- [ ] LLM 프롬프트 튜닝 (개선 제안 생성)
  ```
  프롬프트 예시:
  "다음 문장의 격식 오류를 수정하세요: {sentence}
  수정안을 JSON으로 출력: {\"original\": \"...\", \"suggestion\": \"...\"}"
  ```
- [ ] 규칙 사전 확장 (100개 이상)

#### Frontend Developer
- [ ] 텍스트 하이라이트 구현
  ```
  라이브러리 옵션:
  - react-highlight-words
  - 직접 구현 (CSS + string manipulation)
  ```
- [ ] 분석 이력 페이지
- [ ] 시각화 (격식 분포 파이 차트)

**체크포인트 (금요일):**
- 텍스트 하이라이트 작동 확인
- 이력 기능 테스트
- UI/UX 피드백 수집

---

### Week 6: 관리자 기능 및 최적화
**목표:** 규칙 사전 관리, 성능 최적화

#### Backend Lead
- [ ] 관리자 API (규칙 사전 CRUD)
  ```
  POST /api/admin/rules - 규칙 추가
  GET /api/admin/rules - 규칙 목록
  PUT /api/admin/rules/:id - 규칙 수정
  DELETE /api/admin/rules/:id - 규칙 삭제
  ```
- [ ] 성능 최적화
  - API 응답 시간 측정
  - 캐싱 구현 (Redis 등, 선택)
  - DB 쿼리 최적화

#### AI/NLP Engineer
- [ ] 엣지 케이스 처리
  - 짧은 문장 (< 10자)
  - 긴 문장 (> 1000자)
  - 특수 문자, 이모지 포함
- [ ] 규칙 사전 품질 검증
- [ ] 정확도 재평가 (85%+ 목표)

#### Frontend Developer
- [ ] 관리자 페이지
  - 규칙 사전 테이블
  - 추가/수정/삭제 폼
- [ ] 통계 대시보드 (차트)
- [ ] 반응형 디자인 (모바일 대응)

**체크포인트 (금요일):**
- 관리자 기능 작동 확인
- 성능 목표 달성 확인 (1000자 2초 이내)
- 코드 리뷰 및 리팩토링

---

### Week 7: 테스트 및 문서화
**목표:** 테스트 작성, 문서 완성

#### Backend Lead
- [ ] 단위 테스트 작성 (선택한 테스트 프레임워크)
  ```python
  # tests/test_api.py (예시)
  def test_analyze_formality():
      response = client.post("/api/analyze/formality", json={
          "text": "안녕하세요. 보고드립니다."
      })
      assert response.status_code == 200
      assert response.json()["formality_level"] == "formal"
  ```
- [ ] API 테스트 (Postman Collection 등)
- [ ] 커버리지 측정 (80%+ 목표)
- [ ] API 문서 보완 (자동 생성 문서 기반)

#### AI/NLP Engineer
- [ ] 알고리즘 테스트
- [ ] 정확도 평가 리포트 작성
  - 격식 분류: 정확도 X%
  - 일관성 감지: F1 Score X
- [ ] 알고리즘 설명 문서 (ALGORITHM.md)
  ```markdown
  # 격식 분석 알고리즘
  
  ## 1. 종결어미 추출
  ...
  
  ## 2. 격식 수준 판단
  ...
  ```

#### Frontend Developer
- [ ] 프론트엔드 테스트 (선택한 테스트 프레임워크)
  ```
  예시:
  - 컴포넌트 렌더링 테스트
  - API 연동 테스트 (모킹)
  - 사용자 인터랙션 테스트
  ```
- [ ] 사용자 가이드 작성 (USER_GUIDE.md)
  - 스크린샷 포함
  - 기능별 사용법

#### 전체 팀
- [ ] README.md 작성
  ```markdown
  # 한국어 텍스트 격식/톤 분석 시스템
  
  ## 프로젝트 소개
  ...
  
  ## 설치 방법
  ```bash
  git clone ...
  cd ...
  docker-compose up
  ```
  
  ## 사용 방법
  ...
  ```

**체크포인트 (금요일):**
- 테스트 커버리지 80%+ 달성
- 문서화 80% 완료
- 버그 리스트 정리

---

### Week 8: 최종 마무리 및 발표
**목표:** 배포, 최종 발표

#### 월요일~수요일: 최종 통합 및 버그 수정
- [ ] 통합 테스트 (E2E)
- [ ] 버그 수정
- [ ] 코드 정리 (린팅, 포매팅)
- [ ] 주석 및 문서 보완

#### 수요일: Docker 배포 준비
- [ ] Dockerfile 최적화
  ```dockerfile
  # backend/Dockerfile (예시 - Python 기준)
  FROM python:3.11-slim
  WORKDIR /app
  COPY requirements.txt .
  RUN pip install -r requirements.txt
  COPY . .
  CMD ["python", "main.py"]  # 또는 선택한 서버 실행 명령
  ```

- [ ] Docker Compose 최종 설정
  ```yaml
  version: '3.8'
  
  services:
    backend:
      build: ./backend
      ports:
        - "8000:8000"
      environment:
        - DATABASE_URL=${DATABASE_URL}
        - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      depends_on:
        - db
    
    frontend:
      build: ./frontend
      ports:
        - "3000:3000"
      depends_on:
        - backend
    
    db:
      image: postgres:15  # 또는 선택한 DB 이미지
      environment:
        - POSTGRES_USER=${DB_USER}
        - POSTGRES_PASSWORD=${DB_PASSWORD}
        - POSTGRES_DB=${DB_NAME}
      volumes:
        - db_data:/var/lib/postgresql/data
  
  volumes:
    db_data:
  ```

- [ ] 배포 가이드 작성 (DEPLOYMENT.md)

#### 목요일: 발표 준비
- [ ] 발표 자료 작성 (PPT 25~30장)
  - 프로젝트 소개 (5장)
  - 시스템 아키텍처 (5장)
  - 핵심 기능 (8장)
  - 성능 평가 (4장)
  - 시연 (3장)
  - 개발 과정 (3장)
  - 향후 계획 (2장)

- [ ] 데모 시나리오 작성
  ```
  1. 격식 혼용 문서 시연
     입력: "안녕하세요. 보고드립니다. 매출은 증가했어요."
     결과: 격식 혼용 감지, 수정 제안
  
  2. 높임법 오류 시연
     입력: "사장님이 말했습니다."
     결과: 주체 높임 누락 감지
  
  3. 일관성 좋은 문서 시연
     입력: "안녕하세요. 보고드립니다. 매출은 증가했습니다."
     결과: 높은 품질 점수
  ```

- [ ] 리허설 (1~2회)

#### 금요일: 최종 발표
- **오전:** 최종 점검
- **오후:** 발표 및 데모
- **저녁:** 팀 회고 (Retrospective)

**제출물 체크리스트:**
- [ ] GitHub Repository (코드)
- [ ] Docker Compose (원클릭 실행)
- [ ] 테스트 코드 (커버리지 80%+)
- [ ] README.md
- [ ] API 문서 (Swagger)
- [ ] 알고리즘 문서 (ALGORITHM.md)
- [ ] 사용자 가이드 (USER_GUIDE.md)
- [ ] 배포 가이드 (DEPLOYMENT.md)
- [ ] 발표 자료 (PPT)
- [ ] 데모 영상 (백업용)

---

## 3. 형상관리 전략

### 3.1 Git 브랜치 전략 (GitHub Flow)

```
main (프로덕션)
  ↑
  └─ develop (개발 통합)
       ↑
       ├─ feature/backend-api (Backend Lead)
       ├─ feature/nlp-analysis (AI/NLP Engineer)
       └─ feature/frontend-ui (Frontend Developer)
```

**브랜치 규칙:**
- `main`: 배포 가능한 안정 버전만
- `develop`: 개발 중인 기능 통합
- `feature/*`: 개별 기능 개발

**브랜치 네이밍:**
```
feature/backend-api-design
feature/nlp-formality-analyzer
feature/frontend-result-card
bugfix/issue-123
hotfix/critical-bug
```

### 3.2 커밋 메시지 규칙

**포맷:**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**타입:**
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서 수정
- `style`: 코드 포맷팅 (기능 변경 없음)
- `refactor`: 리팩토링
- `test`: 테스트 추가
- `chore`: 빌드/설정 변경

**예시:**
```
feat(backend): Add formality analysis API

- Implement POST /api/analyze/formality endpoint
- Integrate Kiwi morphology analyzer
- Add unit tests

Closes #12
```

### 3.3 코드 리뷰 프로세스

**Pull Request (PR) 규칙:**
1. **작업 완료 후 PR 생성**
   - `feature/*` → `develop`
   - 제목: `[Feature] Add formality analyzer`
   - 설명: 변경 사항, 테스트 결과, 스크린샷

2. **리뷰어 지정**
   - 최소 1명 이상 (가능하면 2명)
   - 관련 영역 담당자 우선

3. **리뷰 기준**
   - 코드 품질 (가독성, 중복 제거)
   - 테스트 포함 여부
   - 문서 업데이트 여부
   - 스타일 가이드 준수

4. **Approve 후 Merge**
   - Squash and Merge 권장
   - Merge 후 feature 브랜치 삭제

**PR 템플릿:**
```markdown
## 변경 사항
- [ ] 격식 분석 API 추가
- [ ] 테스트 추가

## 테스트
- 단위 테스트: 통과
- 통합 테스트: 통과

## 스크린샷
(있으면 첨부)

## 체크리스트
- [ ] 코드 린팅 통과
- [ ] 테스트 작성
- [ ] 문서 업데이트
```

### 3.4 이슈 트래킹

**GitHub Issues 사용:**
- 버그: `[Bug] 격식 분석 오류`
- 기능: `[Feature] 텍스트 하이라이트`
- 문서: `[Docs] README 작성`

**라벨 체계:**
- `bug`: 버그
- `feature`: 새 기능
- `documentation`: 문서
- `high-priority`: 우선순위 높음
- `help-wanted`: 도움 필요

**이슈 템플릿:**
```markdown
## 버그 설명
격식 분석 시 빈 문자열 입력하면 500 에러

## 재현 방법
1. 텍스트 입력창에 공백만 입력
2. 분석 버튼 클릭
3. 500 에러 발생

## 기대 동작
빈 문자열에 대한 에러 메시지 표시

## 환경
- OS: Windows 10
- Browser: Chrome 120
```

### 3.5 버전 관리

**Semantic Versioning (SemVer):**
```
v0.1.0 - Week 2: 기본 프로토타입
v0.2.0 - Week 4: 중간 발표 버전
v0.3.0 - Week 6: 고급 기능 추가
v1.0.0 - Week 8: 최종 릴리스
```

**Git Tag:**
```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

### 3.6 파일 구조 (프로젝트 루트)

```
formality-analyzer/
├── .github/
│   ├── workflows/           # CI/CD (선택)
│   │   └── test.yml
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── ISSUE_TEMPLATE/
│       ├── bug_report.md
│       └── feature_request.md
├── backend/
│   ├── app/                 # 또는 src/ (프레임워크에 따라 조정)
│   │   ├── main             # 앱 진입점
│   │   ├── routers/
│   │   ├── services/
│   │   │   ├── morphology_service
│   │   │   ├── formality_analyzer
│   │   │   ├── consistency_checker
│   │   │   └── llm_service
│   │   ├── models/
│   │   └── database
│   ├── tests/
│   ├── requirements.txt     # 또는 package.json, Gemfile 등
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   └── App
│   ├── public/
│   ├── package.json
│   └── Dockerfile
├── docs/
│   ├── ALGORITHM.md          # 알고리즘 설명
│   ├── USER_GUIDE.md         # 사용자 가이드
│   └── DEPLOYMENT.md         # 배포 가이드
├── data/
│   ├── formality_rules.json  # 종결어미 사전
│   └── honorific_dict.json   # 높임 표현 사전
├── docker-compose.yml
├── .gitignore
├── README.md
└── LICENSE
```

> 💡 **Note:** 파일 확장자는 선택한 기술 스택에 따라 달라집니다.  
> 예: TypeScript 사용 시 `.ts/.tsx`, JavaScript 사용 시 `.js/.jsx`

---

## 4. 개발 환경 및 도구 (아웃라인)

> ⚠️ **중요:** 구체적인 기술 스택은 팀 미팅(Week 1)에서 최종 결정됩니다.  
> 아래는 제안서 기준 권장사항이며, 팀원들의 경험과 선호도에 따라 조정 가능합니다.

### 4.1 기술 스택 (권장안)

#### 공통
- **형상관리:** Git + GitHub
- **배포:** Docker + Docker Compose
- **협업:** Slack/Discord, Notion/GitHub Projects

#### Backend
**언어 & 프레임워크 (택 1):**
- ✅ **Python** (제안서 명시)
  - FastAPI (권장 - 비동기, 자동 문서화)
  - Flask (경량, 간단)
  - Django (풀스택, ORM 강력)

**형태소 분석:**
- ✅ **Kiwi** (kiwipiepy) - 제안서 명시
- 대안: KoNLPy, MeCab-ko

**LLM API:**
- ✅ **Anthropic Claude API** (제안서 명시)
- 대안: OpenAI GPT API

**데이터베이스 (택 1):**
- PostgreSQL (권장 - JSON 지원, 확장성)
- MySQL/MariaDB (안정적, 널리 사용)
- SQLite (간단, 배포 용이 - MVP에 적합)

#### Frontend
**프레임워크 (택 1):**
- ✅ **React** (제안서 명시)
  - TypeScript 사용 권장
- Vue.js (학습 곡선 낮음)
- Svelte (빠름, 간결)

**UI 라이브러리 (선택):**
- Material-UI (권장 - 완성도 높음)
- Ant Design (기업용 UI)
- Tailwind CSS (유틸리티 우선)
- 직접 CSS (자유도 높음)

**상태 관리 (선택):**
- Zustand (간단)
- Redux Toolkit (복잡한 상태)
- React Query (서버 상태)

### 4.2 기술 스택 결정 프로세스

**Week 1 월요일 - 기술 스택 결정 미팅:**

1. **각 팀원의 경험 공유**
   - Backend Lead: Python 경험도? FastAPI vs Flask?
   - NLP Engineer: 형태소 분석기 사용 경험?
   - Frontend Dev: React vs Vue? TypeScript vs JavaScript?

2. **결정 기준**
   - 팀원 경험도 (학습 곡선 고려)
   - 프로젝트 요구사항 (성능, 확장성)
   - 개발 생산성 (라이브러리, 문서화)
   - 배포 편의성

3. **합의 도출**
   - 최종 기술 스택 확정
   - README에 기록
   - 개발 환경 세팅 가이드 작성

**예시 결정 템플릿:**
```markdown
# 기술 스택 결정 (2026.03.03)

## Backend
- 언어: Python 3.11
- 프레임워크: FastAPI
- 이유: 비동기 처리, Swagger 자동 생성, 팀원 모두 경험 있음

## Frontend  
- 프레임워크: React + TypeScript
- UI: Material-UI
- 이유: 생태계 풍부, TypeScript로 타입 안정성 확보

## Database
- 선택: SQLite (개발) → PostgreSQL (프로덕션 고려)
- 이유: 초기에는 SQLite로 빠르게 개발, 필요시 PostgreSQL 전환

## 형태소 분석
- 선택: Kiwi (제안서 명시)
```

### 4.3 필수 공통 도구

**모든 팀원이 설치해야 하는 것:**
- Git (버전 관리)
- Docker & Docker Compose (배포)
- IDE/Editor (VS Code 권장, 각자 선호도 존중)
- 커뮤니케이션 도구 (Slack 또는 Discord)

### 4.4 코드 품질 도구 (선택)

**Backend (Python 예시):**
- Linter: flake8, pylint
- Formatter: black, autopep8
- Type Checker: mypy (선택)

**Frontend (JavaScript/TypeScript 예시):**
- Linter: ESLint
- Formatter: Prettier
- Type Checker: TypeScript

> 💡 **Tip:** Week 1에 pre-commit hook 설정하면 코드 품질 자동 관리 가능

### 4.5 테스트 프레임워크 (선택)

**Backend:**
- pytest (Python 권장)
- unittest (Python 내장)

**Frontend:**
- Jest (React 기본)
- Vitest (빠름, 최신)
- React Testing Library

> 📌 **목표:** 테스트 커버리지 80% 이상 (제안서 명시)

---

## 5. 의사소통 및 협업

### 5.1 정기 미팅

**일일 스탠드업 (매일 10:00, 15분)**
- 어제 한 일
- 오늘 할 일
- 블로커 (막히는 부분)

**주간 회고 (매주 금요일 17:00, 30분)**
- 잘한 점 (Keep)
- 개선할 점 (Problem)
- 다음 주 계획 (Try)

### 5.2 커뮤니케이션 채널

**Slack (또는 Discord):**
- `#general`: 전체 공지
- `#dev-backend`: Backend 논의
- `#dev-frontend`: Frontend 논의
- `#dev-nlp`: AI/NLP 논의
- `#random`: 잡담

**Google Drive:**
- 회의록
- 발표 자료
- 참고 문서

**GitHub:**
- 코드 리뷰
- 이슈 트래킹
- 문서

### 5.3 문서화 원칙

**코드 주석:**
- 함수/클래스에 docstring 필수
- 복잡한 로직에만 인라인 주석
- 한글 또는 영어 (일관성 유지)

**README:**
- 프로젝트 소개
- 설치 방법
- 사용 방법
- 라이선스

**API 문서:**
- FastAPI Swagger 자동 생성
- 각 엔드포인트에 예시 포함

**사용자 가이드:**
- 스크린샷 포함
- 단계별 설명
- FAQ

---

## 6. 프로젝트 종료 후 인수인계

### 6.1 최종 제출물 체크리스트

**코드:**
- [ ] GitHub Repository (Public 또는 Private)
- [ ] 모든 브랜치가 develop 또는 main으로 병합됨
- [ ] 불필요한 파일 제거 (.env, __pycache__, node_modules)
- [ ] .gitignore 정리

**문서:**
- [ ] README.md
- [ ] API 문서 (Swagger)
- [ ] ALGORITHM.md (알고리즘 설명)
- [ ] USER_GUIDE.md (사용자 가이드)
- [ ] DEPLOYMENT.md (배포 가이드)
- [ ] ARCHITECTURE.md (시스템 아키텍처)

**배포:**
- [ ] Dockerfile (backend, frontend)
- [ ] docker-compose.yml (원클릭 실행)
- [ ] .env.example (환경 변수 템플릿)

**테스트:**
- [ ] 단위 테스트 (커버리지 80%+)
- [ ] 통합 테스트
- [ ] 성능 평가 리포트

**발표:**
- [ ] PPT 파일 (PDF 변환)
- [ ] 데모 영상 (MP4, 백업용)

### 6.2 인수인계 문서 (HANDOVER.md)

```markdown
# 인수인계 문서

## 프로젝트 개요
- 프로젝트명: 한국어 텍스트 격식 수준 및 톤 자동 분석 시스템
- 기간: 2026.03 ~ 2026.06 (8주)
- 팀원: [이름1], [이름2], [이름3]

## 실행 방법

### 1. 사전 준비
- Docker 설치
- Git 설치
- Anthropic API 키 발급

### 2. 실행
\```bash
git clone [repo-url]
cd formality-analyzer
cp .env.example .env
# .env 파일에 API 키 입력
docker-compose up
\```

### 3. 접속
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- Swagger 문서: http://localhost:8000/docs

## 주요 파일 설명

### Backend
- `app/main.py`: FastAPI 앱 진입점
- `app/services/formality_analyzer.py`: 격식 분석 로직
- `app/services/llm_service.py`: LLM API 연동

### Frontend
- `src/pages/AnalysisPage.tsx`: 메인 분석 페이지
- `src/components/analysis/ResultCard.tsx`: 결과 표시 컴포넌트

### 데이터
- `data/formality_rules.json`: 종결어미 사전 (100+ 항목)
- `data/honorific_dict.json`: 높임 표현 사전 (50+ 항목)

## 알려진 이슈
- [ ] 1000자 이상 텍스트 처리 시 속도 저하 (최적화 필요)
- [ ] 특수 문자 많은 텍스트에서 형태소 분석 오류 (Kiwi 한계)

## 향후 개선 방향
- Fine-tuned 모델 도입 (API 비용 절감)
- 실시간 분석 (WebSocket)
- 모바일 앱 (React Native)

## 연락처
- [이름1]: [이메일]
- [이름2]: [이메일]
- [이름3]: [이메일]
```

### 6.3 공유용 패키지

**압축 파일 구성:**
```
formality-analyzer-v1.0.0.zip
├── code/                    # 전체 소스코드
├── docs/                    # 문서
├── docker-compose.yml       # 배포 파일
├── .env.example             # 환경 변수 예시
├── HANDOVER.md              # 인수인계 문서
├── presentation.pdf         # 발표 자료
└── demo-video.mp4           # 데모 영상
```

### 6.4 GitHub Repository 정리

**최종 정리 체크리스트:**
- [ ] develop → main 최종 병합
- [ ] 릴리스 태그 생성 (v1.0.0)
- [ ] GitHub Release 페이지 작성
  - 릴리스 노트
  - 다운로드 파일 첨부
  - 주요 기능 하이라이트
- [ ] README에 배지 추가
  ```markdown
  ![Version](https://img.shields.io/badge/version-1.0.0-blue)
  ![License](https://img.shields.io/badge/license-MIT-green)
  ```

### 6.5 오픈소스 공개 (선택)

**공개 전 준비:**
- [ ] 민감 정보 제거 (API 키, 개인 정보)
- [ ] LICENSE 파일 추가 (MIT 또는 Apache 2.0)
- [ ] CONTRIBUTING.md 작성
- [ ] CODE_OF_CONDUCT.md 작성

---

## 7. 리스크 관리

### 7.1 주요 리스크 및 대응

| 리스크 | 영향 | 확률 | 대응 방안 |
|--------|------|------|-----------|
| API 비용 초과 | 높음 | 중간 | 캐싱, 무료 티어 모니터링 |
| 팀원 이탈 | 높음 | 낮음 | 역할 교차 학습, 문서화 철저 |
| 형태소 분석 정확도 부족 | 중간 | 중간 | 규칙 기반 보완, LLM 활용 |
| 일정 지연 | 중간 | 중간 | 주간 진행률 체크, 우선순위 조정 |
| 기술적 난관 | 중간 | 중간 | 전문가 자문, Stack Overflow |

### 7.2 비상 계획 (Contingency Plan)

**시나리오 1: API 비용 초과**
- Plan A: 캐싱 강화 (Redis)
- Plan B: 무료 LLM (Ollama) 로컬 실행
- Plan C: 규칙 기반 전환

**시나리오 2: 일정 지연 (Week 6 시점)**
- Plan A: 비핵심 기능 축소 (관리자 페이지)
- Plan B: UI 간소화
- Plan C: MVP만 완성

---

## 8. 성공 기준

### 8.1 필수 목표 (Must Have)

- [ ] 격식 수준 분석 정확도 85% 이상
- [ ] 일관성 문제 감지 F1 Score 0.8 이상
- [ ] 1000자 텍스트 2초 이내 분석
- [ ] Docker Compose 원클릭 실행
- [ ] 테스트 커버리지 80% 이상
- [ ] README 및 API 문서 완성

### 8.2 선택 목표 (Nice to Have)

- [ ] 관리자 페이지 (규칙 사전 관리)
- [ ] 분석 이력 통계 대시보드
- [ ] 반응형 디자인 (모바일)
- [ ] CI/CD 파이프라인

### 8.3 평가 지표

**기술적 지표:**
- 분석 정확도 (%)
- 응답 시간 (ms)
- 코드 커버리지 (%)

**프로젝트 관리:**
- 일정 준수율 (%)
- 커밋 빈도 (주당)
- 코드 리뷰 참여도

**팀워크:**
- 미팅 참석률 (%)
- PR 리뷰 속도 (시간)
- 이슈 해결 속도 (일)

---

## 부록

### A. 참고 자료

**한국어 NLP:**
- Kiwi 문서: https://github.com/bab2min/Kiwi
- 한국어 높임법 연구: https://www.jstor.org/stable/42922193

**FastAPI:**
- 공식 문서: https://fastapi.tiangolo.com/
- Tutorial: https://fastapi.tiangolo.com/tutorial/

**React + TypeScript:**
- React 공식 문서: https://react.dev/
- TypeScript 핸드북: https://www.typescriptlang.org/docs/

**Docker:**
- Docker 문서: https://docs.docker.com/
- Docker Compose: https://docs.docker.com/compose/

### B. 용어 사전

- **격식 (Formality):** 상황에 따른 언어 사용의 공식성 정도
- **종결어미 (Sentence Ending):** 문장을 끝맺는 어미 (~습니다, ~해요 등)
- **주체 높임 (Subject Honorification):** 주어를 높이는 표현 (~시, 께서)
- **객체 높임 (Object Honorification):** 목적어를 높이는 표현 (드리다, 여쭙다)
- **형태소 분석 (Morphological Analysis):** 단어를 최소 의미 단위로 분해
- **일관성 (Consistency):** 문서 내에서 격식이 통일되어 있는 정도

---

### Note

- 본 문서는 **Anthropic Claude**을 활용하여 작성되었으며 작성자의 감수를 거쳤지만 일부 기술적으로 불분명한 부분이 있을 수 있습니다.
- 기본 포맷은 **(주)도담솔루션** 개발 프로젝트 계획서를 준용하였으므로 이를 고려해 참고하시기 바랍니다.

---

**문서버전:** 1.0  
**작성자:** (주)도담솔루션 개발팀장 홍준용  
**작성일:** 2026.02.12  
**최종수정:** 2026.03.23
